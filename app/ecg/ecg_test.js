'use strict';

describe('myApp.ecg module', function() {

  beforeEach(module('myApp.ecg'));

  describe('ecg controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('EcgCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});